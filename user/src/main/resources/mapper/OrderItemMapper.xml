<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="zhenghc.mapper.OrderItemMapper">

    <insert id="insert" parameterType="zhenghc.entity.OrderItem">
        insert into order_item
        (order_id, product_type, product_id, product_name, product_price, quantity, subtotal)
        values
            (#{orderId}, #{productType}, #{productId}, #{productName}, #{productPrice}, #{quantity}, #{subtotal})
    </insert>
</mapper>