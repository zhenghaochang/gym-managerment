<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="zhenghc.mapper.MemberCourseMapper">

    <insert id="insert">
        insert into gym.member_course (course_id, course_name, user_id, user_name, status, order_num)
        value
        (#{courseId},#{courseName},#{userId},#{userRealName},#{status},#{orderNum})
    </insert>
</mapper>