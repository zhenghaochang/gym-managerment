# 健身房管理系统开发进度

## 项目信息
- 前端：Vue3 + Element Plus + Pinia + Vue Router
- 后端：Spring Boot 2.7.18 + Spring Security + JWT + Redis
- 数据库：MySQL

---

## ✅ 已完成功能

### 后端部分
1. **Spring Security + JWT 集成**
   - SecurityConfig 配置（禁用 CSRF、Session，配置白名单）
   - JwtAuthenticationTokenFilter 过滤器（从 Redis 获取用户信息）
   - JwtUtil 工具类（生成/解析 token，密钥至少 32 字符）
   - RedisUtil 工具类（存取用户信息）

2. **登录接口**
   - 路径：POST `/api/user/login`
   - 返回格式：`{ resCode: "00", resMsg: "操作成功", result: { token, user } }`
   - 用户权限通过 SQL 关联查询（user → user_role → role_permission → permission）
   - 权限存储在 User 对象的 `permissions` 字段（List<String>）

3. **注册接口**
   - 路径：POST `/api/user/regist`
   - 邮箱验证码功能（Thymeleaf 模板邮件）
   - 获取验证码：POST `/api/user/getEmailCode`
   - 验证码存储在 Redis，5 分钟有效

4. **修改密码接口**
   - 路径：POST `/api/user/changePassword`
   - 参数：`ChangePasswordDTO { username, oldPassword, newPassword }`

5. **个人中心接口**
   - 查询：POST `/api/myCenter/list`
   - 修改：POST `/api/myCenter/update`（birthday 字段为 java.sql.Date，需传 yyyy-MM-dd）

6. **我的订单接口**
   - 查询：POST `/api/myOrder/list`

7. **预约记录接口**
   - 查询：POST `/api/bookedRecord/list`
   - 删除：POST `/api/bookedRecord/delete`

### 前端部分
1. **登录页面** (`login.vue`)
   - 渐变背景 + 卡片设计
   - 表单校验 + 验证码
   - 对接后端登录接口
   - 登录成功保存 token、userInfo、permissions 到 localStorage

2. **注册页面** (`register.vue`)
   - 邮箱验证码（调用后端接口发送）
   - 表单校验（用户名、密码、邮箱必填，手机号选填）
   - 注册成功跳转登录页

3. **主布局系统**
   - 左侧菜单：`MainMenu.vue`（深色系，动态权限菜单，logo 点击跳转首页）
   - 顶部导航：`Navigation.vue`（动态面包屑 + 居中励志标语 + 修改密码 + 退出登录）
   - 登录注册页独立布局（无菜单导航）

4. **动态权限菜单**
   - Pinia Store：`permission.js`（支持持久化恢复）
   - 菜单配置：`menuConfig.js`
   - 页面刷新后自动从 localStorage 恢复菜单
   - 菜单项高度 68px，字号 17px，图标 24px

5. **API 架构**
   - 统一服务 `apiService.js`（端口 8081，超时 30 秒）
   - 统一拦截器：自动添加 Bearer token、错误处理、401 跳转
   - 响应拦截器已解包 `response.data`，使用 `res.resCode` 而非 `res.data.resCode`

6. **路由守卫**
   - 未登录访问系统页 → 跳转登录
   - 已登录访问登录页 → 跳转 Dashboard
   - 退出登录清除 token 和 Store

7. **办理会员卡** (`CreateCard.vue`)
   - 8种卡片样式，分为时长卡和次数卡两个分区
   - 响应式 `auto-fill` 布局，Element Plus 图标
   - 对接 `POST /card/list` 获取卡种列表
   - 对接 `POST /card/createOrder` 购买会员卡（参数：userId, cardId, paymentMethod）

8. **我的会员卡** (`MyCard.vue`)
   - 对接 `POST /myCard/list` 获取我的会员卡列表
   - 进度计算使用 `durationDays` 和 `totalTimes`
   - 仅展示卡片信息，无操作按钮（停卡/转卡/续费功能已移除）

9. **购买课程** (`PurchaseCourse.vue`)
   - 两个分类：私教课(courseType=2) 和 团课(courseType=1)
   - 对接 `POST /course/list` 和 `POST /course/buyCourse`
   - 购买数量固定为 1

10. **我的课程** (`MyCourse.vue`)
    - 对接 `POST /myCourse/list` 和 `POST /myCourse/dropClass`
    - 退课申请需填写原因（不超过200字）
    - 状态标签：1=待预约, 2=已使用, 3=已退课

11. **退课审批** (`ApplicationApproval.vue`)
    - 对接 `POST /courseManager/list` 和 `POST /courseManager/updateStatus`
    - 通过：发送 `{ ...row, status: 2 }`，驳回：发送 `{ ...row, status: 3, refuseReason }`
    - appType 标签：1=退课申请, 2=预约取消申请

12. **团课预约** (`CourseBooking.vue`)
    - 周视图排班表，支持前后翻周（仅可向前翻）
    - 对接 `POST /booking/scheduleList`、`POST /booking/booking`、`POST /booking/courseCancel`
    - 今天高亮，过期日期灰色，recordStatus：1=已预约, 2=申请取消中

13. **私教预约** (`PrivateBooking.vue`)
    - 对接 `POST /booking/privateList` 和 `POST /booking/bookingPrivate`
    - 状态：1=待预约, 2=已预约

14. **我的申请** (`MyApplication.vue`)
    - 对接 `POST /myApplication/list`
    - 审批驳回行整行标红，驳回原因仅在弹窗中展示

15. **预约记录** (`BookedRecord.vue`)
    - 对接 `POST /bookedRecord/list` 和 `POST /bookedRecord/delete`
    - 状态：1=已预约, 2=申请取消中, 3=已取消
    - 删除确认弹窗："删除之后将永久无法找回，确定删除吗？"

16. **个人中心** (`MyCenter.vue`)
    - 左侧头像卡片 + 右侧信息网格
    - 对接 `POST /myCenter/list` 查询和 `POST /myCenter/update` 修改
    - birthday 字段传 yyyy-MM-dd 格式（后端 java.sql.Date）

17. **我的订单** (`MyOrder.vue`)
    - 对接 `POST /myOrder/list`
    - 支付方式：1=支付宝, 2=微信
    - 支付状态：1=待支付, 2=支付成功, 3=支付失败, 4=已退款
    - 订单状态：1=待处理, 2=已完成, 3=已取消

18. **导航栏** (`Navigation.vue`)
    - 动态面包屑（根据 menuConfig 和当前路由自动生成）
    - 居中励志标语
    - 修改密码弹窗（旧密码 + 新密码 + 确认密码）
    - 对接 `POST /user/changePassword`，成功后跳转登录页

19. **Dashboard 首页** (`Dashboard.vue`)
    - 硬汉风格设计：深色横幅 + TRAIN HARD 装饰文字 + 随机健身语录
    - 白色整体背景 + 偏灰蓝组件色调
    - 数据统计卡片（会员卡/课程/预约/订单数量，实时 API 数据）
    - 快捷操作入口（6个常用功能，军事风编号）
    - 动态问候语（根据时间段变化）

20. **课程设置** (`course/CourseSetting.vue`)
    - 课程列表按类型优先、ID其次排序
    - 对接 `POST /courseManager/courseSetting`（编辑）、`POST /courseManager/courseAdd`（新增）、`POST /courseManager/deleteCourse`（删除）

21. **排期管理** (`course/ScheduleManage.vue`)
    - 对接 `POST /courseManager/insertSchedule`（新增）、`POST /courseManager/scheduleUpdate`（修改）、`POST /courseManager/deleteSchedule`（删除）
    - 排期参数：courseId, coachId, weekDay, startTime, endTime, location, maxCapacity, status
    - 列表按周几优先、上课时间其次排序
    - 表格列使用 `min-width` 均匀铺满
    - 新增/修改时前端冲突检测：同一天 + 同一地点 + 时间段重叠则阻止提交并提醒

22. **预约管理** (`course/BookingManage.vue`)
    - 对接 `POST /courseManager/BookingList` 获取所有预约记录
    - 表格列：id, userId, memberCourseId, scheduleId, bookingDate, bookingTime（null显示"—"）, status（标签）
    - 前端筛选：用户ID、排期ID、预约状态
    - 预约状态：1=已预约, 2=申请取消中, 3=已取消

---

## 📝 技术要点

### 菜单权限代码（会员角色）
```
member:myCenter        - 个人中心 (/member/myCenter) 
member:order           - 我的订单 (/member/order)
member:create-card     - 办理会员卡 (/create-card)
course:purchase        - 购买课程 (/purchase-course)
course:my-card         - 我的会员卡 (/my-card)
member:my-course       - 我的课程 (/member/myCourse)
member:course-booking  - 团课预约 (/member/course-booking)
member:course-booking2 - 私教预约 (/member/course-booking2)
member:my-application  - 我的申请 (/member/myApplication)
member:booked-record   - 预约记录 (/member/bookedRecord)
```

### 用户类型
- 1: 超级管理员
- 2: 管理员
- 3: 教练
- 4: 会员

### 接口返回码
- `00`: 成功
- `01`: 业务错误
- `11`: 用户权限非法（需退出登录）
- `99`: 系统错误

### 支付方式
- 1: 支付宝
- 2: 微信

### 课程相关
- 课程类型：1=团课, 2=私教课
- 我的课程状态：1=待预约, 2=已使用, 3=已退课
- 私教预约状态：1=待预约, 2=已预约
- 预约记录状态：1=已预约, 2=申请取消中, 3=已取消
- 申请类型：1=退课申请, 2=预约取消申请
- 申请状态：1=待处理, 2=审批通过, 3=审批驳回
- 团课排班 recordStatus：1=已预约, 2=申请取消中

- 排课状态：1=启用, 0=停用
- 排课周几：1=周一, 2=周二, ..., 7=周日

### 教练ID映射
- 4: 刘强
- 5: 陈静
- 6: 赵刚

### 会员卡状态
- 1: 正常
- 2: 冻结
- 3: 已过期
- 4: 已用完

---

## 📌 注意事项

1. 前端只修改 `gym-front` 目录，后端 `user` 目录手动修改
2. 响应拦截器已解包 `response.data`，使用 `res.resCode` 而非 `res.data.resCode`
3. `userInfo`（含 `realName`, `id`, `username`）从 `localStorage.getItem('userInfo')` 获取
4. 后端 `birthday` 字段为 `java.sql.Date`，必须传 `yyyy-MM-dd` 格式
5. 后端 `nickName` 字段为驼峰命名
6. `ChangePasswordDTO` 字段：`username`, `oldPassword`, `newPassword`
7. JWT 密钥必须至少 32 字符
8. 停卡/转卡/续费功能已移除
9. 我的课表和体测记录菜单项已删除

---

**最后更新：** 2026-02-27
**当前阶段：** 前端所有页面开发完成，全部接口已对接（含课程管理模块）
