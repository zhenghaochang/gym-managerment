# 健身房管理系统开发进度

## 项目信息
- 前端：Vue3 + Element Plus + Pinia + Vue Router
- 后端：Spring Boot 2.7.18 + Spring Security + JWT + Redis
- 数据库：MySQL

---

## ✅ 已完成功能

### 后端部分
1. **Spring Security + JWT 集成**
   - SecurityConfig 配置（禁用 CSRF、Session，配置白名单）
   - JwtAuthenticationTokenFilter 过滤器（从 Redis 获取用户信息）
   - JwtUtil 工具类（生成/解析 token，密钥至少 32 字符）
   - RedisUtil 工具类（存取用户信息）

2. **登录接口**
   - 路径：POST `/api/user/login`
   - 返回格式：`{ resCode: "00", resMsg: "操作成功", result: { token, user } }`
   - 用户权限通过 SQL 关联查询（user → user_role → role_permission → permission）
   - 权限存储在 User 对象的 `permissions` 字段（List<String>）

3. **注册接口**
   - 路径：POST `/api/user/regist`
   - 邮箱验证码功能（Thymeleaf 模板邮件）
   - 获取验证码：POST `/api/user/getEmailCode`
   - 验证码存储在 Redis，5 分钟有效

### 前端部分
1. **登录页面** (`login.vue`)
   - 渐变背景 + 卡片设计
   - 表单校验 + 验证码
   - 对接后端登录接口
   - 登录成功保存 token、userInfo、permissions 到 localStorage

2. **注册页面** (`register.vue`)
   - 邮箱验证码（调用后端接口发送）
   - 表单校验（用户名、密码、邮箱必填，手机号选填）
   - 头像上传（暂未开放）
   - 注册成功跳转登录页

3. **主布局系统**
   - 左侧菜单：`MainMenu.vue`（深色系，动态权限菜单）
   - 顶部导航：`Navigation.vue`（显示真实用户信息）
   - 登录注册页独立布局（无菜单导航）

4. **动态权限菜单**
   - Pinia Store：`permission.js`（支持持久化恢复）
   - 菜单配置：`menuConfig.js`
   - 页面刷新后自动从 localStorage 恢复菜单
   - 角色类型：1-超级管理员、2-管理员、3-教练、4-会员

5. **API 架构**
   - 单体架构：统一服务 `apiService.js`（端口 8081，超时 30 秒）
   - 统一拦截器：自动添加 Bearer token、错误处理、401 跳转
   - API 模块：user.js, member.js, course.js, order.js

6. **路由守卫**
   - 未登录访问系统页 → 跳转登录
   - 已登录访问登录页 → 跳转 Dashboard
   - 退出登录清除 token 和 Store

---

## 🚧 进行中

### 会员功能开发
- [x] 登录功能
- [x] 注册功能
- [x] 权限持久化（刷新页面菜单不丢失）
- [x] 退出登录（调用后端接口清除 Redis）
- [x] 办理会员卡页面（对接后端 `/card/list` 接口）
- [ ] 购买课程页面

---

## 📋 待办功能

### 前端页面
- [ ] Dashboard 页面
- [x] 办理会员卡页面 (`/create-card`) ✅
- [ ] 购买课程页面 (`/purchase-course`)
- [ ] 我的主页 (`/member/home`)
- [ ] 预约记录 (`/member/booking`)
- [ ] 我的课表 (`/member/timetable`)
- [ ] 体测记录 (`/member/measure`)
- [ ] 我的订单 (`/member/order`)

### 后端模块
- [x] 退出登录接口 ✅
- [x] 会员卡种类查询接口 (`/card/list`) ✅
- [ ] 会员卡购买接口
- [ ] 课程管理
- [ ] 订单管理

---

## 📝 技术要点

### 权限代码（会员角色）
```
member:home        - 我的主页
member:booking     - 预约记录
member:timetable   - 我的课表
member:measure     - 体测记录
member:order       - 我的订单
member:create-card - 办理会员卡（独立菜单）
course:purchase    - 购买课程（独立菜单）
```

### 用户类型
- 1: 超级管理员
- 2: 管理员
- 3: 教练
- 4: 会员

### 后端白名单（SecurityConfig）
```java
.antMatchers("/user/login", "/user/regist", "/user/getEmailCode", "/user/logout").permitAll()
```

### 样式风格
- 深色系主题（菜单、导航栏）
- 紫色系主题色 (#667eea, #764ba2)

---

## 📌 注意事项

1. JWT 密钥必须至少 32 字符
2. SecurityConfig 白名单路径不需要加 context-path
3. 邮件模板放在 `resources/templates/email/` 目录
4. 前端只修改 `gym-front` 目录，后端 `user` 目录手动修改
5. 数据库 avatar 字段需要用 LONGTEXT 类型（存 Base64）

---

**最后更新：** 2026-01-03
**当前阶段：** 办理会员卡页面完成，下一步开发购买课程页面
